import styled from "styled-components"

import { COLORS, GRADIENTS, DEVICE } from "@/styles/variables"

export const AdStyle = styled.section`
  display: flex;
  align-items: center;
  justify-content: center;
  .loader-container,
  .ad-block-container {
    margin: 1rem auto 0;
    padding: 0 2rem;
  }
`
