import styled from "styled-components"

import {
  COLORS,
  GRADIENTS,
  DEVICE,
  GRADIENTS_VERTICALS,
} from "@/styles/variables"

export const AdBlockStyle = styled.div`
  img {
    width: 100%;
    height: auto;
  }
`
